import 'package:flutter/material.dart';

class FilenameComponent extends StatelessWidget {
  const FilenameComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
