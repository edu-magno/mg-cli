import 'package:flutter/material.dart';

class DaleWidget extends StatelessWidget {
  const DaleWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
